apply plugin: 'android'

android {
    compileSdkVersion 17
    buildToolsVersion "18.0.1"

    defaultConfig {
        minSdkVersion 7
        targetSdkVersion 17
    }
}

dependencies {
    compile 'com.android.support:support-v4:13.0.0'

    compile project(':ScribbleLib')
}
